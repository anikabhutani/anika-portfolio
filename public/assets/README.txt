Drop your real files here with these exact names (referenced in the code):
  - anika_headshot.jpeg      (About section photo)
  - smart_clinical_img.png   (Smart Clinical project screenshot)
  - resume.pdf               (downloaded by the terminal's 'resume' command)

Optional:
  - terminal-icon.png        (replaces the built-in SVG glyph on the floating
                               terminal launcher button — see
                               src/components/FloatingTerminalButton.tsx for
                               the two lines to swap if you add this)

Any other project screenshots you add: update the `image` field for that
project in src/lib/data.ts to point at /assets/<filename>.
